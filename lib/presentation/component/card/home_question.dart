import 'package:flutter/material.dart';

class HomeQuestion extends StatelessWidget {
  const HomeQuestion({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
