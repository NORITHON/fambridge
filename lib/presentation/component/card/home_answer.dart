import 'package:flutter/material.dart';

class HomeAnswer extends StatelessWidget {
  const HomeAnswer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
