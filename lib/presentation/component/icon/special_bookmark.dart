import 'package:flutter/material.dart';

class SpecialBookmark extends StatelessWidget {
  const SpecialBookmark({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
