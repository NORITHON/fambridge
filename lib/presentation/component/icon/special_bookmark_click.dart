import 'package:flutter/material.dart';

class SpecialBookmarkClick extends StatelessWidget {
  const SpecialBookmarkClick({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
