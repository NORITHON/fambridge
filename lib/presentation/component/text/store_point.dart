import 'package:flutter/material.dart';

class StorePoint extends StatelessWidget {
  const StorePoint({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
