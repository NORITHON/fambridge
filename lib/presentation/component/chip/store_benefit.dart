import 'package:flutter/material.dart';

class StoreBenefit extends StatelessWidget {
  const StoreBenefit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
