import 'package:flutter/material.dart';

class StoreMembership extends StatelessWidget {
  const StoreMembership({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
