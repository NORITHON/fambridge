import 'package:flutter/material.dart';

class HomeLockState extends StatelessWidget {
  const HomeLockState({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
