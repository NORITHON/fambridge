import 'package:flutter/material.dart';

class StoreDownload extends StatelessWidget {
  const StoreDownload({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
